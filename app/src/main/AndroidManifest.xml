<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reseauxsociaux">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReseauxSociaux">
        <activity android:name=".modifier_profil" />
        <activity android:name=".amis" />
        <activity android:name=".profil" />
        <activity android:name=".Search"></activity>
        <activity android:name=".Selection" />
        <activity android:name=".login_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".create_account" />
        <activity android:name=".pwd_forgot" />
    </application>

</manifest>